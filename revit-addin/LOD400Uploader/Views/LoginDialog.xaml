<Window x:Class="LOD400Uploader.Views.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LOD 400 - Sign In" 
        Height="380" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    
    <Window.Resources>
        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1D4ED8"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#94A3B8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="TextBlock" x:Key="LinkText">
            <Setter Property="Foreground" Value="#2563EB"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="TextDecorations" Value="Underline"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#1D4ED8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0">
            <TextBlock Text="LOD 400 Delivery" FontSize="24" FontWeight="Bold" Foreground="#111827"/>
            <TextBlock Text="Sign in to your account" FontSize="14" Foreground="#6B7280" Margin="0,8,0,0"/>
        </StackPanel>
        
        <StackPanel Grid.Row="1" Margin="0,24,0,0">
            <TextBlock Text="Email" FontSize="13" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,8"/>
            <TextBox x:Name="EmailTextBox" FontSize="14" Padding="12,10" 
                     Background="White" BorderBrush="#D1D5DB" BorderThickness="1"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2" Margin="0,16,0,0">
            <TextBlock Text="Password" FontSize="13" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,8"/>
            <PasswordBox x:Name="PasswordBox" FontSize="14" Padding="12,10" 
                         Background="White" BorderBrush="#D1D5DB" BorderThickness="1"/>
            <TextBlock x:Name="ErrorText" Text="" FontSize="12" Foreground="#DC2626" Margin="0,8,0,0" 
                       Visibility="Collapsed"/>
        </StackPanel>
        
        <StackPanel Grid.Row="3" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Don't have an account? " FontSize="12" Foreground="#6B7280"/>
                <TextBlock x:Name="SignUpLink" Text="Sign up" FontSize="12" Style="{StaticResource LinkText}"
                           MouseDown="SignUpLink_MouseDown"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <TextBlock x:Name="ForgotPasswordLink" Text="Forgot password?" FontSize="12" Style="{StaticResource LinkText}"
                           MouseDown="ForgotPasswordLink_MouseDown"/>
            </StackPanel>
        </StackPanel>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Padding="16,10" Background="White" Foreground="#374151"
                    BorderBrush="#D1D5DB" BorderThickness="1" Margin="0,0,12,0" Click="CancelButton_Click"/>
            <Button x:Name="LoginButton" Content="Sign In" Style="{StaticResource PrimaryButton}" 
                    Click="LoginButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
